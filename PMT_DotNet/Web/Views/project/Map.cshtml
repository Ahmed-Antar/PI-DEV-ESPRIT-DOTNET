@using Jmelosegui.Mvc.GoogleMap

@{
    ViewBag.Title = "Map";
}

<h2>Map</h2>

@(
        Html.GoogleMap()
            .Name("map")
            .Height(500)
            .Markers(m => m.Add().Title("Hello World!"))
)


@section scripts
    {
    @(Html.GoogleMap().ScriptRegistrar())
}
